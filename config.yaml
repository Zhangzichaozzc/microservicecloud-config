info:
  name: config.info.version=3
spring:
  profiles: dev
  application:
    name: cloud-config-client
server:
  port: 3355
---
info:
  name: config.info.version=3
spring:
  profiles: test
  application:
    name: cloud-config-client
server:
  port: 3356
